% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schedule.R
\name{on_schedule}
\alias{on_schedule}
\title{Schedule an action on a cron-style time expression}
\usage{
on_schedule(cron, action, id = NULL, tz = "")
}
\arguments{
\item{cron}{Character. A 5-field cron expression
(\code{"min hour dom mon dow"}). See Details.}

\item{action}{A zero-argument function or a one-sided formula \code{~ expr}.}

\item{id}{Optional character ID. Auto-generated when \code{NULL}.}

\item{tz}{Time zone for schedule evaluation. Default \code{""} (system).}
}
\value{
Invisibly returns the schedule ID. Pass to \code{\link[=unwatch]{unwatch()}} to cancel.
}
\description{
Registers a recurring in-session timer that fires \code{action} whenever the
current time matches the cron expression. The timer runs on R's event loop
via \code{later::later()}: it does not block the session and needs no system
cron or root access.
}
\details{
The returned ID is compatible with \code{\link[=unwatch]{unwatch()}} so teardown is consistent
with filesystem watchers.

Cron fields (left to right): minute (0-59), hour (0-23), day-of-month
(1-31), month (1-12), day-of-week (0-6, 0 = Sunday).

Supported syntax per field:
\describe{
\item{\code{*}}{Every value.}
\item{\code{N}}{Exactly N.}
\item{\code{*/N}}{Every N-th value, stepping from the field minimum.}
\item{\code{N,M,...}}{Explicit list.}
\item{\code{N-M}}{Inclusive range.}
}
}
\examples{
\dontrun{
on_schedule("0 8 * * *", action = ~ fetch_cbn_rates())
on_schedule("*/15 9-17 * * 1-5",
            action = function() log_step("Heartbeat check"))
id <- on_schedule("* * * * *", action = ~ message(Sys.time()))
unwatch(id)
}

}
\seealso{
\code{\link[=unwatch]{unwatch()}}, \code{\link[=on_arrive]{on_arrive()}}
}
