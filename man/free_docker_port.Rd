% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maintenance.R
\name{free_docker_port}
\alias{free_docker_port}
\title{Free Up a Blocked Port (Windows)}
\usage{
free_docker_port(port, timeout = 30, verbose = TRUE)
}
\arguments{
\item{port}{Integer TCP port to clear (1-65535).}

\item{timeout}{Numeric. Seconds before the command is killed. Default \code{30}.}

\item{verbose}{Logical. Print what was found and killed (default \code{TRUE}).}
}
\value{
Invisibly, a list with \code{ok}, \code{status}, \code{stdout}, \code{stderr}.
}
\description{
Finds and terminates the Windows process(es) holding a TCP port, using
PowerShell's \code{Get-NetTCPConnection}. Useful when Docker cannot bind a
host port because another application is already listening on it.
}
\details{
Terminating a process can cause unsaved work to be lost. System PIDs
(0 and 4) are never touched.
}
\examples{
\dontrun{
free_docker_port(8000)
free_docker_port(5432)
}
}
