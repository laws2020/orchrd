% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/artifact.R
\name{store_artifact}
\alias{store_artifact}
\title{Store a pipeline output as a named artifact}
\usage{
store_artifact(
  x,
  name,
  format = "rds",
  dir = .artifact_dir(),
  overwrite = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{x}{The R object to save.}

\item{name}{Character. A unique name for this artifact.}

\item{format}{Character. Storage format: \code{"rds"} (default), \code{"parquet"},
\code{"csv"}, or \code{"json"}. Only \code{"rds"} and \code{"parquet"} preserve R types
faithfully; \code{"csv"} and \code{"json"} are lossy for factors, dates, and
list-columns.}

\item{dir}{Character. Artifact directory. Defaults to
\code{tools::R_user_dir("orchrd", "cache")}.}

\item{overwrite}{Logical. Replace an existing artifact with the same
name (default \code{TRUE}).}

\item{verbose}{Logical. Print confirmation (default \code{TRUE}).}
}
\value{
\code{x} invisibly so the function can be used at the end of a
\code{\link[=pipe_exec]{pipe_exec()}} step without breaking the flow.
}
\description{
Saves an R object to disk with metadata tracking. Supports multiple
formats: \code{"rds"} (any R object), \code{"parquet"} (data frames, requires
\code{arrow}), \code{"csv"} (data frames), and \code{"json"}.
}
\details{
The artifact is saved with a metadata sidecar (\code{.meta.json}) recording
the name, format, dimensions (for data frames), timestamp, and size.
Use \code{\link[=load_artifact]{load_artifact()}} to retrieve it and \code{\link[=list_artifacts]{list_artifacts()}} to see
what is stored.

Designed to sit at the end of a \code{\link[=pipe_exec]{pipe_exec()}} step so every significant
output is tracked and reloadable without re-running the pipeline.
}
\examples{
\dontrun{
pipe_exec(
  fetch  = ~ get_data("source"),
  clean  = ~ remove_na(.x),
  save   = ~ store_artifact(.x, "nga_cpi_clean", format = "parquet")
)

model <- lm(value ~ year, data = df)
store_artifact(model, "nga_cpi_model")

df    <- load_artifact("nga_cpi_clean")
model <- load_artifact("nga_cpi_model")
}
}
\seealso{
\code{\link[=load_artifact]{load_artifact()}}, \code{\link[=list_artifacts]{list_artifacts()}}, \code{\link[=delete_artifact]{delete_artifact()}}
}
