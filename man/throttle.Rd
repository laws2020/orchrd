% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/throttle.R
\name{throttle}
\alias{throttle}
\title{Throttle a watcher's callback (fire-first, suppress for N ms)}
\usage{
throttle(id, ms = 1000)
}
\arguments{
\item{id}{Character. Watcher ID to throttle.}

\item{ms}{Numeric. Suppression window in milliseconds. Default \code{1000}.}
}
\value{
Invisibly \code{TRUE}.
}
\description{
Wraps the existing callback so it fires immediately on the first event, then
suppresses subsequent firings for \code{ms} milliseconds. This is the
complement of the built-in debounce (which fires at the trailing edge).
}
\examples{
\dontrun{
id <- on_arrive("~/cbn_feeds/", action = function(event, path) {
  fetch_cbn_rates(path)
})
throttle(id, ms = 30000)  # fire once, then ignore re-drops for 30s
}

}
\seealso{
\code{\link[=guard]{guard()}}, \code{\link[=on_arrive]{on_arrive()}}
}
