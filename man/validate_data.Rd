% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validate_data}
\alias{validate_data}
\title{Run multiple validation assertions on a data frame}
\usage{
validate_data(data, ..., verbose = TRUE)
}
\arguments{
\item{data}{A data frame to validate.}

\item{...}{Named one-sided formulas (\code{name = ~ expr}). Each expression
should call \code{\link[=assert_schema]{assert_schema()}}, \code{\link[=assert_range]{assert_range()}}, \code{\link[=assert_no_na]{assert_no_na()}}, or
return \code{TRUE} on success and throw on failure. Use \code{.x} (or \code{data}) to
refer to the data frame inside each formula.}

\item{verbose}{Logical. Print a pass message if all checks succeed
(default \code{TRUE}).}
}
\value{
The \code{data} argument invisibly if all checks pass. Throws an
error listing every failed check if any fail.
}
\description{
Runs any combination of \code{\link[=assert_schema]{assert_schema()}}, \code{\link[=assert_range]{assert_range()}}, and
\code{\link[=assert_no_na]{assert_no_na()}} against a data frame and collects every failure before
stopping. This gives you a complete picture of what is wrong rather than
halting on the first problem.
}
\details{
IMPORTANT: each rule must be a one-sided formula (\code{~ expr}) so that its
evaluation is deferred until inside \code{validate_data()}. If you pass a bare
\verb{assert_*()} call (not wrapped in \code{~}), R evaluates it eagerly at the call
site and it will halt on the first failure instead of collecting all of
them. \code{validate_data()} warns if it detects a pre-evaluated argument.
}
\examples{
\dontrun{
pipe_exec(
  fetch    = ~ get_data("source"),

  validate = ~ validate_data(.x,
    schema = ~ assert_schema(.x, c("country", "year", "value")),
    ranges = ~ assert_range(.x,  "value", min = 0, max = 1000),
    nas    = ~ assert_no_na(.x,  c("country", "year"))
  ),

  export   = ~ save_output(.x, "data/out.parquet")
)
}
}
\seealso{
\code{\link[=assert_schema]{assert_schema()}}, \code{\link[=assert_range]{assert_range()}}, \code{\link[=assert_no_na]{assert_no_na()}}
}
