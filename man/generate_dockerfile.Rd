% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/builders.R
\name{generate_dockerfile}
\alias{generate_dockerfile}
\alias{generate_r_dockerfile}
\title{Auto-generate an optimized, reproducible Dockerfile for any project}
\usage{
generate_dockerfile(
  stack = c("auto", "r-shiny", "static", "node"),
  packages = character(),
  r_version = "4.4.1",
  node_version = "20",
  port = NULL,
  sysdeps = c("libxml2-dev", "libssl-dev", "libcurl4-openssl-dev"),
  dir = ".",
  overwrite = FALSE
)

generate_r_dockerfile(...)
}
\arguments{
\item{stack}{Character. One of \code{"auto"} (default), \code{"r-shiny"},
\code{"static"}, or \code{"node"}.}

\item{packages}{Character vector of R packages to add (r-shiny only).
Default \code{character()}; shiny-verse already ships shiny + tidyverse.}

\item{r_version}{Character. Tag for \code{rocker/shiny-verse}, e.g. \code{"4.4.1"}.}

\item{node_version}{Character. Major Node version tag, e.g. \code{"20"}.}

\item{port}{Integer. Port the app listens on. Defaults per stack:
\code{3838} (r-shiny), \code{80} (static), \code{3000} (node).}

\item{sysdeps}{Character vector of apt system libraries (r-shiny only).}

\item{dir}{Character. Directory to write the \code{Dockerfile} into. Also the
directory scanned when \code{stack = "auto"}. Default \code{"."}.}

\item{overwrite}{Logical. Overwrite an existing \code{Dockerfile} (default
\code{FALSE}).}

\item{...}{Passed through to \code{\link[=generate_dockerfile]{generate_dockerfile()}}.}
}
\value{
Invisibly returns the path to the written \code{Dockerfile}.
}
\description{
Writes a \code{Dockerfile} tailored to the project stack. Supported stacks:
}
\details{
\describe{
\item{\code{"r-shiny"}}{Shiny app on \strong{\code{rocker/shiny-verse}} (Shiny +
tidyverse preinstalled). Extra packages install as PPM binaries. The
app binds to \verb{0.0.0.0} and the Shiny port is exposed.}
\item{\code{"static"}}{Plain HTML/CSS/JS served by \strong{nginx} (\code{nginx:alpine}).
Your files are copied into the nginx web root and served on port 80.}
\item{\code{"node"}}{Node.js app on \strong{\verb{node:<lts>-alpine}}. Installs
dependencies from \code{package.json} and runs the \code{start} script, binding
to \verb{0.0.0.0}.}
\item{\code{"auto"}}{Detect the stack from files present (\code{app.R}/\code{server.R}
to R, \code{package.json} to node, else static).}
}

Regardless of stack the generated image runs a real web server bound to
\verb{0.0.0.0} and \code{EXPOSE}s its port, so the app is reachable from the host.
}
\examples{
\dontrun{
generate_dockerfile()                       # auto-detect
generate_dockerfile(stack = "static")       # HTML/CSS/JS via nginx
generate_dockerfile(stack = "node", node_version = "20")
generate_dockerfile(stack = "r-shiny", packages = c("plotly"))
# docker build -t my-app . && docker run -p 8080:80 my-app
}

}
\seealso{
\code{\link[=optimize_build_speed]{optimize_build_speed()}}, \code{\link[=minify_r_dependencies]{minify_r_dependencies()}},
\code{\link[=secure_secrets]{secure_secrets()}}
}
