% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch_exec.R
\name{batch_combine}
\alias{batch_combine}
\title{Row-bind the successful data-frame results of a batch_exec() run}
\usage{
batch_combine(results, verbose = TRUE)
}
\arguments{
\item{results}{A named list returned by \code{\link[=batch_exec]{batch_exec()}}.}

\item{verbose}{Logical. Warn about skipped non-data-frame results
(default \code{TRUE}).}
}
\value{
A single data frame with a leading \code{.task} column, or an empty
data frame if there is nothing to combine.
}
\description{
Collects every element of a \code{batch_exec()} result list whose task
succeeded and whose \code{result} is a data frame, adds a \code{.task} column
identifying the source task, and \code{rbind}s them into a single data frame.
Successful non-data-frame results are skipped with a warning.
}
\details{
Column handling is tolerant: frames are aligned by name (union of all
columns), with missing columns filled with \code{NA}, so slightly divergent
schemas still combine.
}
\seealso{
\code{\link[=batch_exec]{batch_exec()}}
}
