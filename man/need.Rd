% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/need.R
\name{need}
\alias{need}
\title{Assert that a pipeline's environment is ready}
\usage{
need(
  cmd = character(),
  env = character(),
  file = character(),
  dir = character(),
  pkg = character(),
  r_version = NULL,
  verbose = TRUE
)
}
\arguments{
\item{cmd}{Character vector. System commands that must be on PATH
(e.g. \code{c("git", "pwsh", "gh")}). Windows \code{.cmd}/\code{.bat}/\code{.exe}
wrappers are resolved the same way as in \code{\link[=run_cmd]{run_cmd()}}.}

\item{env}{Character vector. Environment variable names that must be
set and non-empty (e.g. \code{c("AZURE_KEY", "OPENAFRR_REPO")}).}

\item{file}{Character vector. File paths that must exist (a directory
at that path does not satisfy a \code{file} requirement).}

\item{dir}{Character vector. Directory paths that must exist.}

\item{pkg}{Character vector. R package names that must be installed.}

\item{r_version}{Character. Minimum R version required, e.g. \code{"4.2.0"}.
\code{NULL} (default) skips this check.}

\item{verbose}{Logical. Print a success message if all checks pass
(default \code{TRUE}).}
}
\value{
Invisibly returns \code{TRUE} if all checks pass. Throws a
descriptive error listing every missing item if any check fails.
}
\description{
Checks that required system commands, environment variables, files,
directories, and R packages all exist before a pipeline starts. If
anything is missing it stops with a clear, actionable error message
listing every missing item, not just the first one.
}
\details{
Call \code{need()} at the top of any script that will run unattended or in
CI. It is far better to fail loudly at the start than to fail silently
halfway through a pipeline after 10 minutes of work.
}
\examples{
\dontrun{
need(
  cmd  = c("git", "pwsh", "gh"),
  env  = c("OPENAFRR_REPO", "AZURE_KEY"),
  file = "inst/ps/fetch.ps1",
  pkg  = c("arrow", "readxl")
)
need(r_version = "4.2.0")
}
}
