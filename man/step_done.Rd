% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checkpoint.R
\name{step_done}
\alias{step_done}
\title{Check whether a checkpoint has already been saved}
\usage{
step_done(name, dir = .checkpoint_dir(), max_age = Inf)
}
\arguments{
\item{name}{Character. The checkpoint name to check.}

\item{dir}{Character. Checkpoint directory.}

\item{max_age}{Numeric. Optional maximum age in seconds. If supplied and
the checkpoint is older than this, it is treated as absent (returns
\code{FALSE}), so a stale checkpoint from a previous run won't short-circuit
a fresh one. Default \code{Inf} (never expires).}
}
\value{
Logical. \code{TRUE} if a usable checkpoint exists, \code{FALSE} otherwise.
}
\description{
Returns \code{TRUE} if a checkpoint file exists for \code{name}. Use this at the
start of a step to skip expensive work that has already been done.
}
\examples{
\dontrun{
if (step_done("nga_fetch")) {
  df <- resume_pipeline("nga_fetch")
} else {
  df <- get_data("NGA"); checkpoint(df, "nga_fetch")
}

# Ignore checkpoints older than one hour
if (step_done("nga_fetch", max_age = 3600)) { }
}
}
\seealso{
\code{\link[=checkpoint]{checkpoint()}}, \code{\link[=resume_pipeline]{resume_pipeline()}}, \code{\link[=clear_checkpoint]{clear_checkpoint()}}
}
