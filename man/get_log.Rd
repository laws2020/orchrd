% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/log_step.R
\name{get_log}
\alias{get_log}
\title{Retrieve the in-memory session log}
\usage{
get_log()
}
\value{
A data frame with columns \code{timestamp}, \code{session}, \code{level},
\code{message}. Returns an empty (0-row) data frame with the same columns
if nothing has been logged yet.
}
\description{
Returns all log entries written since the session started (or since the
last \code{\link[=log_config]{log_config()}} call with \code{reset = TRUE}) as a tidy data frame.
}
\examples{
\dontrun{
log_step("step one")
log_step("something suspicious", level = "warn")
df <- get_log()
df[df$level \%in\% c("warn", "error"), ]
}
}
\seealso{
\code{\link[=log_config]{log_config()}} to write logs to a file.
}
