% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/notify.R
\name{notify}
\alias{notify}
\title{Send a notification when a pipeline finishes}
\usage{
notify(
  result,
  on_success = "Pipeline complete: {n_steps} steps, {elapsed}s",
  on_failure = "Pipeline failed. Step(s) with errors: {failed_steps}",
  via = "console",
  webhook = NULL,
  title = "orchrd pipeline",
  timeout = 10,
  ...
)
}
\arguments{
\item{result}{An \code{orchrd_pipeline} object from \code{\link[=pipe_exec]{pipe_exec()}}.}

\item{on_success}{Character. Message to send on full success.}

\item{on_failure}{Character. Message to send when any step failed.}

\item{via}{Character vector. One or more of \code{"console"} (default),
\code{"desktop"}, \code{"slack"}, \code{"teams"}.}

\item{webhook}{An \code{orchrd_secret} or character. Webhook URL for Slack or
Teams. Use \code{\link[=secret]{secret()}} to pass this safely.}

\item{title}{Character. Notification title. Default \code{"orchrd pipeline"}.}

\item{timeout}{Numeric. Seconds before a webhook request is abandoned.
Default \code{10}.}

\item{...}{Reserved for future channel-specific arguments.}
}
\value{
The \code{result} object, invisibly, so \code{notify()} can end a chain.
}
\description{
Sends a message when an \code{orchrd_pipeline} result is passed to it.
Designed to be piped after \code{\link[=pipe_exec]{pipe_exec()}} so unattended pipelines signal
completion without manual checking.
}
\details{
Supports desktop notifications (all platforms), Slack webhooks, Microsoft
Teams webhooks, and plain console output. More than one channel may be
supplied to \code{via}; all channels except \code{"console"} require a webhook or
the relevant system tool.

Message strings support glue-style interpolation. Available variables:
\itemize{
\item \code{{ok}} - TRUE/FALSE
\item \code{{n_steps}} - total number of steps
\item \code{{n_failed}} - number of failed steps
\item \code{{elapsed}} - total elapsed seconds across all steps
\item \code{{failed_steps}} - comma-separated names of failed steps
}
}
