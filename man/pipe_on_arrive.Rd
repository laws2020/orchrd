% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orchrd_bridge.R
\name{pipe_on_arrive}
\alias{pipe_on_arrive}
\title{Fire an orchrd pipeline when a file arrives}
\usage{
pipe_on_arrive(
  path,
  pipeline,
  pattern = NULL,
  recursive = FALSE,
  debounce = 500L,
  guard_fns = list(),
  id = NULL,
  ...
)
}
\arguments{
\item{path}{Directory to watch.}

\item{pipeline}{A function (or orchrd pipeline object) to run.}

\item{pattern}{Optional filename regex.}

\item{recursive}{Watch subdirectories? Default \code{FALSE}.}

\item{debounce}{Milliseconds debounce. Default \code{500L}.}

\item{guard_fns}{Optional named list of zero-argument guard functions.
All must return \code{TRUE} for the pipeline to fire.
Example: \code{list(disk = disk_ok, net = net_ok)}.}

\item{id}{Optional watcher ID.}

\item{...}{Additional arguments forwarded to \code{\link[=run_bg]{run_bg()}}.}
}
\value{
Invisibly returns the watcher ID.
}
\description{
Combines \code{\link[=on_arrive]{on_arrive()}} with \code{\link[=run_bg]{run_bg()}} so that a complete pipeline is
dispatched in the background the moment a matching file lands in \code{path}.
}
\details{
The file path is injected into the pipeline call as the named argument
\code{.file}, so your pipeline can optionally consume it.
}
\examples{
\dontrun{
pipe_on_arrive(
  "~/nbs_drops/",
  pattern  = "\\\\.xlsx$",
  pipeline = function(.file) {
    opennaijR::fetch("NGA", "labour_force", file = .file)
  },
  guard_fns = list(disk = disk_ok, net = net_ok)
)
}

}
\seealso{
\code{\link[=pipe_on_change]{pipe_on_change()}}, \code{\link[=on_arrive]{on_arrive()}}, \code{\link[=run_bg]{run_bg()}}
}
