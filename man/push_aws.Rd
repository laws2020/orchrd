% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/push_aws.R
\name{push_aws}
\alias{push_aws}
\title{Sync a local directory to S3 via the AWS CLI}
\usage{
push_aws(
  local_dir,
  bucket,
  prefix = "",
  profile = NULL,
  region = NULL,
  delete = FALSE,
  dryrun = FALSE,
  error_ok = FALSE
)
}
\arguments{
\item{local_dir}{Character. Local folder to upload.}

\item{bucket}{Character. Target S3 bucket name.}

\item{prefix}{Character. Optional key prefix (path inside the bucket).}

\item{profile}{Character. Optional AWS named profile.}

\item{region}{Character. Optional AWS region.}

\item{delete}{Logical. Remove S3 objects not present locally (mirror).}

\item{dryrun}{Logical. Show what would happen without uploading.}

\item{error_ok}{Logical. Return result even on failure (default FALSE).}
}
\value{
An \code{orchrd_result} object.
}
\description{
Thin wrapper over \code{run_cmd()} that shells out to the AWS CLI's
\code{s3 sync}. Requires the \code{aws} CLI installed and credentials
configured (profile, \code{~/.aws}, or env vars).
}
\examples{
\dontrun{
push_aws("dist/", bucket = "my-site", prefix = "static", profile = "prod")
}
}
