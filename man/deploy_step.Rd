% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/with_rollback.R
\name{deploy_step}
\alias{deploy_step}
\title{Create a reversible deployment step}
\usage{
deploy_step(deploy_expr, revert_expr = NULL, label = "", verbose = TRUE)
}
\arguments{
\item{deploy_expr}{The deployment expression.}

\item{revert_expr}{The revert (undo) expression. \code{NULL} means nothing
to undo.}

\item{label}{Character. Label for log messages.}

\item{verbose}{Logical. Log rollback activity (default \code{TRUE}).}
}
\value{
The value of \code{deploy_expr} if it succeeds.
}
\description{
A convenience wrapper around \code{\link[=with_rollback]{with_rollback()}} for the common pattern
where a deployment step writes something (a file, a tag, a remote
resource) and the rollback deletes it.
}
\examples{
\dontrun{
pipe_exec(
  upload = ~ deploy_step(
    deploy_expr = upload_to_server("data/out.parquet"),
    revert_expr = delete_from_server("data/out.parquet"),
    label       = "upload parquet"
  )
)
}
}
\seealso{
\code{\link[=with_rollback]{with_rollback()}}
}
