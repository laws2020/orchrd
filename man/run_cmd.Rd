% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_cmd.R
\name{run_cmd}
\alias{run_cmd}
\title{Run a system command cleanly}
\usage{
run_cmd(
  cmd,
  args = character(),
  dir = ".",
  env = character(),
  timeout = 60,
  echo = FALSE,
  error_ok = FALSE
)
}
\arguments{
\item{cmd}{Character. The command to run, e.g. \code{"git status"}.
For arguments containing spaces or quotes, use \code{args} instead.}

\item{args}{Character vector. Arguments passed separately — safer for
paths with spaces or special characters.}

\item{dir}{Character. Working directory. Defaults to current directory.}

\item{env}{Named character vector. Extra environment variables. These are
\emph{merged} into the current environment (PATH etc. are preserved).}

\item{timeout}{Numeric. Seconds before the process is killed. Default 60.
Use \code{Inf} for long-running operations (large pushes, uploads).}

\item{echo}{Logical. Stream output in real-time (default FALSE).}

\item{error_ok}{Logical. Return result even on failure (default FALSE).}
}
\value{
An S3 object of class \code{orchrd_result} with fields:
\code{$ok}, \code{$status}, \code{$stdout}, \code{$stderr},
\code{$elapsed_sec}, \code{$cmd}.
}
\description{
A unified, cross-platform interface for running system commands.
Returns a structured \code{orchrd_result} object — exit code, stdout,
stderr, and elapsed time.
}
\examples{
\dontrun{
run_cmd("git status")
run_cmd("git", args = c("commit", "-m", "my commit message"))
}
}
