% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/log_step.R
\name{with_log}
\alias{with_log}
\title{Execute an expression with automatic entry and exit logging}
\usage{
with_log(label = "step", expr, level = "info", .data = NULL)
}
\arguments{
\item{label}{Character. Description of the step shown in log messages.}

\item{expr}{An R expression or block \code{{ ... }}.}

\item{level}{Character. Log level for the start and done messages
(default \code{"info"}).}

\item{.data}{List. Extra structured data attached to the log entries.}
}
\value{
The value of \code{expr}. Side effect: two log entries written,
start and done (or failure) with elapsed time.
}
\description{
Wraps a block of code with start and completion log messages, including
elapsed time. On error, logs the failure message before re-throwing.
}
\details{
Drop \code{with_log("label", { ... })} around any block and it becomes fully
observable: timestamped, timed, and failure-safe, without touching the
logic inside.
}
\examples{
\dontrun{
df <- with_log("Fetch source data", {
  get_data("source")
})
}
}
\seealso{
\code{\link[=log_step]{log_step()}} for single messages, \code{\link[=log_config]{log_config()}} to configure
file output.
}
