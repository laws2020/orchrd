% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checkpoint.R
\name{checkpoint}
\alias{checkpoint}
\title{Save a pipeline step result as a checkpoint}
\usage{
checkpoint(value, name, dir = .checkpoint_dir(), verbose = TRUE)
}
\arguments{
\item{value}{Any R object. The value to save (usually \code{.x} inside a
\code{\link[=pipe_exec]{pipe_exec()}} step).}

\item{name}{Character. A unique name for this checkpoint. Used as the
filename on disk.}

\item{dir}{Character. Directory to save the checkpoint.}

\item{verbose}{Logical. Print a confirmation message (default \code{TRUE}).}
}
\value{
The \code{value} argument, invisibly. The pipeline continues
unaffected — even if the save fails (a warning is issued instead of an
error, so a checkpointing problem never aborts a working pipeline).
}
\description{
Writes the current value to disk so the pipeline can resume from this
point if it is interrupted. Designed to wrap the value passing through
\code{\link[=pipe_exec]{pipe_exec()}} — it saves \code{value} and returns it unchanged so the pipeline
continues normally.
}
\details{
The write is \strong{atomic}: the payload is written to a temp file in the same
directory and then renamed into place, so an interrupted write can never
leave a half-written checkpoint that \code{\link[=step_done]{step_done()}} would wrongly treat as
complete.

Checkpoints are stored in \code{tools::R_user_dir("orchrd", "cache")} by
default. Override with the \code{ORCHRD_CHECKPOINT_DIR} environment variable.
}
\examples{
\dontrun{
pipe_exec(
  .init = "NGA",
  fetch = ~ {
    if (step_done("nga_fetch")) return(resume_pipeline("nga_fetch"))
    checkpoint(get_data(.x), "nga_fetch")
  },
  clean = ~ {
    if (step_done("nga_clean")) return(resume_pipeline("nga_clean"))
    checkpoint(remove_na(.x), "nga_clean")
  },
  export = ~ save_output(.x, "data/nga.parquet")
)
}
}
\seealso{
\code{\link[=step_done]{step_done()}}, \code{\link[=resume_pipeline]{resume_pipeline()}}, \code{\link[=clear_checkpoint]{clear_checkpoint()}}
}
