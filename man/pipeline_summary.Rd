% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{pipeline_summary}
\alias{pipeline_summary}
\title{Summarise pipeline performance across multiple runs}
\usage{
pipeline_summary(
  log_file,
  type = c("auto", "session", "pipeline"),
  verbose = TRUE
)
}
\arguments{
\item{log_file}{Character. Path to an NDJSON session log written by
\code{\link[=log_config]{log_config()}} with \verb{file=}, OR a pipe_exec JSON run log written by
\code{\link[=pipe_exec]{pipe_exec()}} with \verb{.log_file=}, OR a directory containing multiple
such files. Multiple paths can be supplied as a character vector.}

\item{type}{Character. \code{"auto"} (default) detects each file's format;
\code{"session"} forces NDJSON session logs; \code{"pipeline"} forces pipe_exec
JSON run logs.}

\item{verbose}{Logical. Print summary to console (default \code{TRUE}).}
}
\value{
A named list with a stable shape (keys always present):
\verb{$runs}, \verb{$success_rate}, \verb{$elapsed}, \verb{$steps}, \verb{$errors}, \verb{$raw}.
}
\description{
Reads the log file(s) produced by \code{\link[=log_config]{log_config()}} and/or
\code{\link[=pipe_exec]{pipe_exec()}} and returns a tidy summary of performance metrics
across runs: success rate, mean and total elapsed time, step-level
timing, and failure patterns.
}
\details{
This is the function to call after a week of scheduled pipeline runs
to understand where time is spent and which steps fail most often.
}
\examples{
\dontrun{
pipeline_summary("logs/")
pipeline_summary(c("logs/run_2026_04_01.json", "logs/run_2026_04_02.json"),
                 type = "pipeline")
pipeline_summary("logs/session.json", type = "session")
}
}
