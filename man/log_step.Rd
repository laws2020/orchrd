% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/log_step.R
\name{log_step}
\alias{log_step}
\title{Log a workflow step}
\usage{
log_step(msg, level = "info", .data = NULL)
}
\arguments{
\item{msg}{Character. The message. Supports cli inline markup and
interpolation, e.g. \code{"Loaded {nrow(df)} rows"}. Interpolation happens
exactly once, against the calling environment.}

\item{level}{Character. One of \code{"debug"}, \code{"info"} (default),
\code{"warn"}, or \code{"error"}.}

\item{.data}{List. Optional structured data attached to the log entry.
Written to the JSON file only, not printed to the console.}
}
\value{
Invisibly returns the log entry as a list.
}
\description{
Logs a message at a given severity level. Printed to the console via
\code{cli} and optionally written to an NDJSON log file configured by
\code{\link[=log_config]{log_config()}}.
}
\details{
Works anywhere: standalone in a script, inside \code{\link[=pipe_exec]{pipe_exec()}} steps, or
inside any function you want to make observable.
}
\examples{
\dontrun{
log_step("Starting data fetch")
log_step("Cache miss - downloading fresh data", level = "warn")
log_step("Connection timed out", level = "error")
log_step("Loaded {nrow(df)} rows from {source}")
log_step("Fetch complete",
         .data = list(source = "endpoint_a", rows = nrow(df)))
}
}
\seealso{
\code{\link[=with_log]{with_log()}} to auto-log an entire block of code.
}
