% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch_exec.R
\name{batch_exec}
\alias{batch_exec}
\title{Execute a list of tasks in controlled parallel batches}
\usage{
batch_exec(
  tasks,
  fn,
  batch_size = 4L,
  mode = c("sequential", "parallel"),
  on_error = c("collect", "stop"),
  combine = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{tasks}{A list or named character vector. Each element is one input
to process.}

\item{fn}{A function that accepts one element of \code{tasks} and returns a
result. Must be a plain R function (not a formula).}

\item{batch_size}{Integer. Maximum number of tasks to run simultaneously
in \code{"parallel"} mode. Ignored in \code{"sequential"} mode. Default \code{4}.}

\item{mode}{Character. \code{"sequential"} (default) runs tasks one at a time
in the current session with progress tracking. \code{"parallel"} runs
\code{batch_size} tasks simultaneously as background jobs via \code{run_bg()}.
Note: \code{"parallel"} requires \code{fn} (and everything it closes over) to be
serialisable with \code{\link[=saveRDS]{saveRDS()}} - use \code{"sequential"} when \code{fn} closes over
session variables, open connections, or external pointers.}

\item{on_error}{Character. \code{"collect"} (default) records failures and
continues. \code{"stop"} halts on the first failure (killing any still-running
jobs in the current batch first).}

\item{combine}{Logical. If \code{TRUE}, \code{rbind} the successful results that are
data frames into a single data frame, returned as the \code{"combined"}
attribute of the result list (and, when every successful result is a
data frame, also accessible via \code{\link[=batch_combine]{batch_combine()}}). A \code{.task} column is
added identifying the source task. Non-data-frame results are skipped
with a warning. Default \code{FALSE}.}

\item{verbose}{Logical. Print batch progress (default \code{TRUE}).}
}
\value{
A named list with one element per task:
\describe{
\item{result}{The return value of \code{fn(task)}, or \code{NULL} on failure.}
\item{ok}{Logical. Whether the task succeeded.}
\item{error}{Character error message, or \code{NA} if successful.}
\item{elapsed_sec}{Numeric. Time taken for this task. In \code{"parallel"}
mode this is approximate wall time measured from when the job's
await began, not the job's true run time.}
}
Names match the names of \code{tasks} (or \code{task_1}, \code{task_2}, ... if
unnamed; duplicates are disambiguated with \code{\link[=make.unique]{make.unique()}}). When
\code{combine = TRUE}, the combined data frame is attached as the
\code{"combined"} attribute.
}
\description{
Processes a list of inputs by applying a function to each. In
\code{"parallel"} mode, \code{batch_size} tasks run simultaneously via \code{run_bg()}
and are awaited with \code{await()}; when one batch completes, the next starts,
continuing until the full list is exhausted. In \code{"sequential"} mode tasks
run one at a time in the current session with progress tracking, and
\code{batch_size} is ignored.
}
\details{
This is the right tool when you have more tasks than you want to run
simultaneously. Running all 54 country fetches at once would overwhelm
the network and the target servers. Running them one-by-one is too slow.
\code{batch_exec()} gives you a controlled middle ground.
}
\examples{
\dontrun{
# Fetch country datasets - 4 at a time in parallel, combined into one df
countries <- c("NGA", "ZAF", "KEN", "GHA", "ETH", "EGY")

results <- batch_exec(
  tasks      = countries,
  fn         = function(iso) get_data(iso),
  batch_size = 4,
  mode       = "parallel",
  combine    = TRUE
)

combined <- attr(results, "combined")   # one data frame, with a .task column

successes <- Filter(function(r) isTRUE(r$ok), results)
failures  <- Filter(function(r) !isTRUE(r$ok), results)
message(length(successes), " succeeded, ", length(failures), " failed")
}
}
\seealso{
\code{run_bg()}, \code{await()}, \code{\link[=pipe_exec]{pipe_exec()}}, \code{\link[=batch_combine]{batch_combine()}}
}
