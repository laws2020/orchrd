% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triggers.R
\name{fire_on}
\alias{fire_on}
\title{Bind an action to a filesystem event}
\usage{
fire_on(
  event,
  path,
  action,
  pattern = NULL,
  recursive = FALSE,
  debounce = 500L,
  id = NULL
)
}
\arguments{
\item{event}{Character. One of "arrive", "change", "leave", "rename",
or the raw equivalents "created", "modified", "deleted",
"renamed".}

\item{path}{Character. Directory (or file) to watch.}

\item{action}{A function accepting (event, path), OR a one-sided formula
~ expr where .path and .event are available as bindings.}

\item{pattern}{Optional regex for matching filenames. NULL matches all.}

\item{recursive}{Watch subdirectories? Default FALSE.}

\item{debounce}{Milliseconds debounce window. Default 500L.}

\item{id}{Optional watcher ID.}
}
\value{
Invisibly returns the watcher ID.
}
\description{
The main trigger API. Declaratively describes: "when THIS event happens
to files matching THIS pattern in THIS location, run THIS action."
}
\examples{
\dontrun{
# Formula syntax - quick one-liners
fire_on("arrive", "~/nbs_drops/", pattern = "\\\\.xlsx$",
        action = ~ message("File arrived: ", .path))

# Function syntax - full pipeline trigger
fire_on("arrive", "~/cbn_feeds/",
        action = function(event, path) {
          opennaijR::fetch("NGA", "inflation")
        })
}

}
\seealso{
\code{\link[=on_arrive]{on_arrive()}}, \code{\link[=on_change]{on_change()}}, \code{\link[=on_leave]{on_leave()}}, \code{\link[=on_rename]{on_rename()}},
\code{\link[=watch_dir]{watch_dir()}}, \code{\link[=watch_file]{watch_file()}}
}
