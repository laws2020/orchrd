% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maintenance.R
\name{clean_docker_storage}
\alias{clean_docker_storage}
\title{Clean Docker Storage Bloat}
\usage{
clean_docker_storage(
  all = FALSE,
  volumes = FALSE,
  dry_run = FALSE,
  confirm = TRUE,
  timeout = 300,
  verbose = TRUE
)
}
\arguments{
\item{all}{Logical. Also remove all unused images, not just dangling
ones (\code{--all}). Default \code{FALSE}.}

\item{volumes}{Logical. Also remove unused volumes (\code{--volumes}). This
can permanently delete data. Default \code{FALSE}.}

\item{dry_run}{Logical. If \code{TRUE}, report what would be removed via
\verb{docker system df} without deleting anything. Default \code{FALSE}.}

\item{confirm}{Logical. Must be \code{TRUE} to actually prune when
\code{volumes = TRUE}. A guardrail against accidental data loss.
Default \code{TRUE} (interactive sessions still proceed; set to \code{FALSE}
in scripts to force an explicit re-check).}

\item{timeout}{Numeric. Seconds before the command is killed. Default
\code{300} (pruning large caches can be slow).}

\item{verbose}{Logical. Print progress and reclaimed-space output
(default \code{TRUE}).}
}
\value{
Invisibly, a list with \code{ok}, \code{status}, \code{stdout}, \code{stderr}.
}
\description{
Removes unused Docker resources to reclaim disk space by running
\verb{docker system prune}. Cross-platform (Windows, Linux, macOS).
}
\details{
By default this removes stopped containers, unused networks, and
dangling images only. Set \code{all = TRUE} to also remove unused images not
associated with any container, and \code{volumes = TRUE} to also remove
unused volumes. Because volumes may contain application data, volume
pruning requires an explicit opt-in.
}
\examples{
\dontrun{
# Safe default: stopped containers, unused networks, dangling images
clean_docker_storage()

# Aggressive: also remove all unused images and volumes
clean_docker_storage(all = TRUE, volumes = TRUE)

# Preview only
clean_docker_storage(dry_run = TRUE)
}
}
