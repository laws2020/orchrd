% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/health_check.R
\name{health_check}
\alias{health_check}
\title{Check that endpoints and services are alive}
\usage{
health_check(
  ...,
  timeout = 10,
  ok_status = 400L,
  types = NULL,
  stop_on_fail = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{...}{Named or unnamed character strings. Each is one endpoint to
check. See Details for how each is classified.}

\item{timeout}{Numeric. Seconds before a probe times out (default \code{10}).}

\item{ok_status}{Integer. HTTP status codes below this are considered
healthy (default \code{400}, so 4xx/5xx are failures). Set to \code{500} to
treat 4xx as reachable-but-erroring.}

\item{types}{Optional named/positional character vector to force the
probe type of a target: one of \code{"http"}, \code{"socket"}, \code{"cmd"}. Length
must match the number of targets, or be \code{NULL} (default, auto-detect).}

\item{stop_on_fail}{Logical. If \code{TRUE}, throws an error after checking
all endpoints if any failed (default \code{FALSE}).}

\item{verbose}{Logical. Print results to the console (default \code{TRUE}).}
}
\value{
A data frame with columns \code{name}, \code{target}, \code{type}, \code{ok},
\code{status}, \code{response_ms}. Invisibly returned so it can be used in a
pipeline.
}
\description{
Sends lightweight probes (HTTP HEAD requests for URLs, socket tests for
hosts, command checks for system tools) and returns a tidy data frame
reporting the status of each. Designed to be called before a pipeline
that depends on external services so you know upfront which sources are
reachable.
}
\details{
Each target is classified as:
\itemize{
\item \strong{URL} — starts with \verb{http://} or \verb{https://} -> HTTP HEAD probe.
\item \strong{host / host:port} — contains a dot or an explicit \verb{:port}
-> TCP socket probe (default port 80).
\item \strong{command} — a bare token with no dot, slash, or colon
-> \code{\link[=Sys.which]{Sys.which()}} lookup.
}
The classification can be forced per target with the \code{types} argument.

By default \code{health_check()} \strong{does not stop} on failure — it reports
every result and lets you decide. Set \code{stop_on_fail = TRUE} to make it
halt the pipeline if any check fails.
}
\examples{
\dontrun{
health_check(
  CBN  = "https://www.cbn.gov.ng/rates/mprates.asp",
  NBS  = "https://nigerianstat.gov.ng"
)

health_check("https://api.example.com", stop_on_fail = TRUE)

health_check(git = "git", pwsh = "pwsh", gh = "gh")
}
}
