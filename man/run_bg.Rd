% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/await.R
\name{run_bg}
\alias{run_bg}
\title{Run a function or pipeline in the background}
\usage{
run_bg(what, ..., label = NULL, packages = "orchrd", supervise = TRUE)
}
\arguments{
\item{what}{A function to run in the background. A non-function value is
wrapped and returned as-is by the job.}

\item{...}{Named arguments forwarded to \code{what} when it is a function.}

\item{label}{Optional human-readable label recorded on the job handle.}

\item{packages}{Character vector of packages to attach in the worker.
Defaults to loading \code{orchrd} so pipeline steps can use its helpers.}

\item{supervise}{Kill the background process if the main session exits?
Default \code{TRUE}.}
}
\value{
An object of class \code{orchrd_job}: a \code{callr} process handle with a
\code{label} and \code{started_at} attribute. It exposes \verb{$kill()}, \verb{$is_alive()},
\verb{$get_result()}, and \verb{$wait()}.
}
\description{
Launches \code{what} in a separate, supervised R process so the calling
session stays free. Returns immediately with a job handle you can pass
to \code{\link[=await]{await()}} to collect the result. Used by \code{\link[=batch_exec]{batch_exec()}} for controlled
parallel execution and by tripwire's \code{pipe_on_arrive()} to dispatch
pipelines when files arrive.
}
\details{
\code{what} may be a function (called in the background with any arguments
supplied via \code{...}) or a zero-argument callable / pipeline object. When a
function is supplied, the \code{...} arguments are forwarded to it, so
\code{run_bg(pipeline, .file = "x.csv")} calls \code{pipeline(.file = "x.csv")} in
the background.
}
\examples{
\dontrun{
job <- run_bg(function(x) x * 2, x = 21)
await(job)   # 42
}
}
\seealso{
\code{\link[=await]{await()}}, \code{\link[=batch_exec]{batch_exec()}}
}
