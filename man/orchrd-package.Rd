% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orchrd-package.R
\docType{package}
\name{orchrd-package}
\alias{orchrd}
\alias{orchrd-package}
\title{orchrd: PowerShell-Inspired Workflow Orchestration for R}
\description{
\code{orchrd} brings PowerShell's workflow philosophy into R:
simple commands, clean outputs, pipeline thinking, retry logic,
and automation-friendly logging.

Designed as infrastructure for the openafrR ecosystem but useful
for any R developer orchestrating scripts, APIs, and system processes.
\subsection{Core functions}{\tabular{ll}{
   Function \tab What it does \cr
   \code{\link[=run_cmd]{run_cmd()}} \tab Run any system command, get structured output \cr
   \code{\link[=run_script]{run_script()}} \tab Run R / Python / PowerShell / bash scripts \cr
   \code{\link[=pipe_exec]{pipe_exec()}} \tab Chain steps into a named, logged pipeline \cr
   \code{\link[=retry]{retry()}} \tab Retry any expression with backoff and jitter \cr
   \code{\link[=log_step]{log_step()}} \tab Log a workflow step with level and timing \cr
   \code{\link[=with_log]{with_log()}} \tab Auto-log entry, exit, and elapsed for any block \cr
   \code{\link[=log_config]{log_config()}} \tab Configure log level and output file \cr
   \code{\link[=get_log]{get_log()}} \tab Retrieve the in-memory session log \cr
   \code{inspect_file()} \tab See full structure of Excel/CSV/PDF before reading \cr
   \code{read_tabs()} \tab Smart multi-tab Excel reader \cr
   \code{sniff_csv()} \tab Detect encoding, delimiter, skip rows before reading \cr
   \code{scan_pdf()} \tab Classify PDF as digital or scanned, detect tables \cr
   \code{read_clean()} \tab One-shot smart reader for any file type \cr
   \code{warm_cache()} \tab Parallel cache warming via PowerShell \cr
   \code{cache_status()} \tab Report on the local dataset cache \cr
   \code{cache_clear()} \tab Remove stale or all cached files \cr
   \code{ps_available()} \tab Check if PowerShell 7 plus is installed \cr
   \code{validate_article()} \tab Validate an article before submission \cr
   \code{submit_article()} \tab Submit an article via git and GitHub PR \cr
   \code{article_status()} \tab Poll PR approval state \cr
   \code{list_articles()} \tab List all your article submissions \cr
   \code{retract_article()} \tab Close a PR and delete the remote branch \cr
}

}

\subsection{Quick start}{

\if{html}{\out{<div class="sourceCode r">}}\preformatted{library(orchrd)

# Run a system command and get structured output
result <- run_cmd("git status")
result$ok
result$stdout

# Full pipeline with logging and retry
log_config(file = "logs/session.json")

pipe_exec(
  fetch  = ~ retry(some_api_call()),
  clean  = ~ dplyr::filter(.x, !is.na(value)),
  export = ~ arrow::write_parquet(.x, "data/output.parquet")
)
}\if{html}{\out{</div>}}
}
}
\seealso{
Useful links:
\itemize{
  \item \url{https://laws2020.github.io/orchrd}
  \item \url{https://github.com/laws2020/orchrd}
  \item Report bugs at \url{https://github.com/laws2020/orchrd/issues}
}

}
\author{
\strong{Maintainer}: Lawrence Garba \email{yayoli@infosights.ng}

Authors:
\itemize{
  \item Lawrence Garba \email{yayoli@infosights.ng}
}

}
\keyword{internal}
