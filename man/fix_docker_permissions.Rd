% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine_repair.R
\name{fix_docker_permissions}
\alias{fix_docker_permissions}
\title{Fix Docker Named Pipe Permissions (Windows)}
\usage{
fix_docker_permissions(
  user = Sys.getenv("USERNAME"),
  timeout = 60,
  verbose = TRUE
)
}
\arguments{
\item{user}{Character. Windows username to add. Defaults to the current
user (\code{USERNAME} env var).}

\item{timeout}{Numeric. Seconds before the PowerShell call is killed.
Default \code{60}.}

\item{verbose}{Logical. Print progress (default \code{TRUE}).}
}
\value{
Invisibly, a list with \code{ok}, \code{status}, \code{stdout}, and \code{stderr}.
}
\description{
Adds the current Windows user to the local \code{docker-users} security group so
Docker Desktop can be used without per-operation elevation.
}
\details{
Windows-only, and \strong{requires an elevated (Administrator) R session} —
modifying local groups is an administrative operation. Unlike the original,
this reports failure instead of silently swallowing a permission error.
}
\examples{
\dontrun{
res <- fix_docker_permissions()
res$ok
}

}
