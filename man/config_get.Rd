% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{config_get}
\alias{config_get}
\title{Retrieve a configuration value}
\usage{
config_get(..., default = .no_default)
}
\arguments{
\item{...}{One or more character keys forming a path through the config.
Single key for top-level values; multiple keys for nested values.}

\item{default}{Any. Value to return if the key is not found. If omitted,
a missing key is an error. Supplying \code{default = NULL} is honored — it
returns \code{NULL} instead of erroring.}
}
\value{
The config value, or \code{default} if the key is not found.
}
\description{
Returns a value from the config loaded by \code{\link[=load_config]{load_config()}}. Supports nested
access by passing multiple keys.
}
\examples{
\dontrun{
config_get("throttle")
config_get("sources", "cbn")
config_get("missing_key", default = "fallback")
config_get("maybe_missing", default = NULL)   # returns NULL, no error
}
}
\seealso{
\code{\link[=load_config]{load_config()}}, \code{\link[=config_all]{config_all()}}, \code{\link[=config_has]{config_has()}}
}
