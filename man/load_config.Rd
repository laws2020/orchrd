% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{load_config}
\alias{load_config}
\title{Load a configuration file into memory}
\usage{
load_config(path, env = NULL, override = TRUE, verbose = TRUE)
}
\arguments{
\item{path}{Character. Path to the config file.}

\item{env}{Character. If the file contains multiple environments
(e.g. \verb{default:}, \verb{production:}), load this one. \code{NULL} (default) loads
the top-level keys. Requesting an environment that does not exist is an
error (no silent fallback to the whole document).}

\item{override}{Logical. If \code{TRUE} (default), the new config replaces any
config already in memory. If \code{FALSE}, the new config is \strong{deep-merged}
under the existing one — existing keys (at any depth) win.}

\item{verbose}{Logical. Print a confirmation message (default \code{TRUE}).}
}
\value{
Invisibly returns the loaded config as a named list.
}
\description{
Reads a YAML, JSON, or \code{.env} format file and stores the values in memory
so they can be retrieved with \code{\link[=config_get]{config_get()}} anywhere in the session. Call
once at the top of a script — then use \code{\link[=config_get]{config_get()}} throughout.
}
\details{
For sensitive values (API keys, passwords) use \code{\link[=secret]{secret()}} instead — it
validates presence, masks the value, and prevents logging. \code{load_config()}
is for non-sensitive configuration: paths, URLs, feature flags, endpoint
names, dataset names.

Supported formats:
\itemize{
\item \strong{YAML} (\code{.yml}, \code{.yaml}) — requires the \code{yaml} package.
\item \strong{JSON} (\code{.json}) — uses \code{jsonlite}, already a dependency.
\item \strong{env} (\code{.env}, \code{config.env}) — \code{KEY=value}, one per line. Lines
starting with \verb{#}, inline \verb{# comments}, and an \code{export } prefix are
handled.
}
}
\examples{
\dontrun{
load_config("config.yml")
config_get("cache_dir")
config_get("sources", "cbn")
load_config("config.yml", env = "production")
}
}
\seealso{
\code{\link[=config_get]{config_get()}}, \code{\link[=config_all]{config_all()}}, \code{\link[=secret]{secret()}} for credentials.
}
