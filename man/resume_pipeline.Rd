% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checkpoint.R
\name{resume_pipeline}
\alias{resume_pipeline}
\title{Load a saved checkpoint value}
\usage{
resume_pipeline(name, dir = .checkpoint_dir(), verbose = TRUE)
}
\arguments{
\item{name}{Character. The checkpoint name to load.}

\item{dir}{Character. Checkpoint directory.}

\item{verbose}{Logical. Print a message when loading (default \code{TRUE}).}
}
\value{
The R object that was passed to \code{\link[=checkpoint]{checkpoint()}}.
}
\description{
Reads the value saved by \code{\link[=checkpoint]{checkpoint()}} and returns it. Typically used
after \code{\link[=step_done]{step_done()}} confirms the checkpoint exists.
}
\examples{
\dontrun{
if (step_done("nga_fetch")) df <- resume_pipeline("nga_fetch")
}
}
\seealso{
\code{\link[=checkpoint]{checkpoint()}}, \code{\link[=step_done]{step_done()}}
}
