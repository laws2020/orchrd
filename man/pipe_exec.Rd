% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipe_exec.R
\name{pipe_exec}
\alias{pipe_exec}
\title{Execute a named pipeline of steps sequentially}
\usage{
pipe_exec(
  ...,
  .init = NULL,
  .on_error = c("stop", "warn", "skip"),
  .log = TRUE,
  .log_file = NULL
)
}
\arguments{
\item{...}{One-sided formulas, named formulas, or functions. Each is one
step. Duplicate names are made unique with a numeric suffix.}

\item{.init}{Any. Initial value passed as \code{.x} to the first step.
Default \code{NULL}.}

\item{.on_error}{One of:
\describe{
\item{\code{"stop"}}{(default) Halt on first failure.}
\item{\code{"warn"}}{Log the failure, pass the previous \code{.x} through,
keep going.}
\item{\code{"skip"}}{Silently keep going with the previous \code{.x}.}
}}

\item{.log}{Logical. Print step progress to the console (default \code{TRUE}).}

\item{.log_file}{Character. Write a JSON run log to this path. Created
or overwritten each run. \code{NULL} (default) disables file logging.}
}
\value{
An S3 object of class \code{orchrd_pipeline}:
\describe{
\item{\verb{$result}}{The final step's output value.}
\item{\verb{$ok}}{Logical. \code{TRUE} if all steps succeeded.}
\item{\verb{$steps}}{Data frame: \code{step}, \code{status}, \code{elapsed_sec}, \code{error}.}
\item{\verb{$log}}{List of raw per-step detail.}
\item{\verb{$elapsed_sec}}{Total wall time across all steps.}
}
}
\description{
The core of \code{orchrd}. Each step receives the output of the previous step
as \code{.x}, logs progress, handles errors, and produces a structured result
you can inspect after the run.
}
\details{
Steps are one-sided formulas (\code{~ expr}) or named formulas
(\code{name = ~ expr}). \code{.x} is the only special symbol - it holds the output
of the previous step.
}
\examples{
\dontrun{
pipe_exec(
  fetch  = ~ get_data("source"),
  clean  = ~ remove_na(.x),
  export = ~ save_parquet(.x, "out.parquet")
)

pipe_exec(
  .init = "NGA",
  fetch = ~ get_data(.x),
  clean = ~ remove_na(.x)
)

pipe_exec(
  a = ~ get_data("source_a"),
  b = ~ get_data("source_b"),
  .on_error = "warn"
)

out <- pipe_exec(
  fetch = ~ get_data("source"),
  clean = ~ remove_na(.x)
)
out$ok
out$result
out$steps
}
}
