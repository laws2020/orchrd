% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{assert_no_na}
\alias{assert_no_na}
\title{Assert that specified columns contain no missing values}
\usage{
assert_no_na(data, cols = NULL)
}
\arguments{
\item{data}{A data frame.}

\item{cols}{Character vector. Column names to check. If \code{NULL}
(default), checks every column in the data frame.}
}
\value{
\code{data} invisibly if no \code{NA}s are found. Throws an error
listing every column with \code{NA} counts otherwise.
}
\description{
Checks that \code{NA} does not appear in any of the listed columns. Reports
exactly how many \code{NA}s are in each column that fails.
}
\examples{
\dontrun{
assert_no_na(df, c("country", "year", "value"))
assert_no_na(df)
}
}
\seealso{
\code{\link[=validate_data]{validate_data()}}, \code{\link[=assert_schema]{assert_schema()}}, \code{\link[=assert_range]{assert_range()}}
}
