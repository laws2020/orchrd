% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/await.R
\name{await}
\alias{await}
\title{Wait for a background job and return its result}
\usage{
await(job, timeout = Inf, verbose = TRUE)
}
\arguments{
\item{job}{An \code{orchrd_job} handle from \code{\link[=run_bg]{run_bg()}}.}

\item{timeout}{Maximum seconds to wait. \code{Inf} (default) waits forever.}

\item{verbose}{Emit a short status line via \code{cli}? Default \code{TRUE}.}
}
\value{
The value returned by the background function.
}
\description{
Blocks until the \code{\link[=run_bg]{run_bg()}} job finishes (or \code{timeout} elapses), then
returns the value the background function produced. If the background
job errored, the error is re-raised in the calling session.
}
\examples{
\dontrun{
job <- run_bg(function() Sys.sleep(1))
await(job)
}
}
\seealso{
\code{\link[=run_bg]{run_bg()}}, \code{\link[=batch_exec]{batch_exec()}}
}
