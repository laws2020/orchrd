% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/with_rollback.R
\name{with_rollback}
\alias{with_rollback}
\title{Execute a step with an automatic rollback on failure}
\usage{
with_rollback(expr, rollback = NULL, label = "", verbose = TRUE)
}
\arguments{
\item{expr}{An R expression - the step to attempt.}

\item{rollback}{An R expression - what to run if \code{expr} fails.
Run for its side effects only; its return value is discarded.
\code{NULL} (the default) means there is nothing to undo.}

\item{label}{Character. Label for log messages. Defaults to an empty
string (no label prefix).}

\item{verbose}{Logical. Log rollback activity (default \code{TRUE}).}
}
\value{
The value of \code{expr} if it succeeds. If \code{expr} fails, the
rollback runs and then the original error is re-thrown.
}
\description{
Wraps a pipeline step with a rollback expression that runs if - and
only if - the step fails. If the step succeeds, the rollback is never
called. If the step fails, the rollback runs and then the error
propagates normally (so \code{\link[=pipe_exec]{pipe_exec()}}'s \code{.on_error} logic still applies).
}
\details{
Designed for deployment-style workflows where steps have side effects
that need to be undone if a later step fails. Common patterns:
\itemize{
\item Copy a file, rollback by deleting it
\item Push a git tag, rollback by deleting the remote tag
\item Deploy to a server, rollback by reverting to the previous version
\item Write to a database, rollback by deleting the inserted rows
}

If the rollback expression itself fails, its error is caught and
reported, but the \emph{original} step error is always the one re-thrown,
so a failing rollback never masks the real cause.
}
\examples{
\dontrun{
pipe_exec(
  build = ~ with_rollback(
    build_package(),
    rollback = clean_build_dir(),
    label    = "build"
  ),
  deploy = ~ with_rollback(
    push_to_production(.x),
    rollback = revert_to_previous_release(),
    label    = "deploy"
  )
)
}
}
\seealso{
\code{\link[=deploy_step]{deploy_step()}}, \code{\link[=pipe_exec]{pipe_exec()}}
}
