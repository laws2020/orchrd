% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_script.R
\name{run_script}
\alias{run_script}
\title{Run an external script file}
\usage{
run_script(
  path,
  args = character(),
  interpreter = NULL,
  dir = NULL,
  env = character(),
  timeout = 300,
  echo = TRUE,
  error_ok = FALSE
)
}
\arguments{
\item{path}{Character. Path to the script file.}

\item{args}{Character vector. Arguments passed to the script.}

\item{interpreter}{Character. Override the detected interpreter,
e.g. \code{"python3"}, \code{"pwsh"}, \code{"Rscript"}.}

\item{dir}{Character. Working directory. Defaults to the script's
parent directory - almost always the right choice.}

\item{env}{Named character vector. Extra environment variables.}

\item{timeout}{Numeric. Seconds before kill (default \code{300}).
Scripts take longer than single commands.}

\item{echo}{Logical. Stream output to the R console (default \code{TRUE}
for scripts - you usually want to see progress).}

\item{error_ok}{Logical. Return result even on failure (default \code{FALSE}).}
}
\value{
An \code{orchrd_result} object. See \code{\link[=run_cmd]{run_cmd()}} for field details.
}
\description{
A single function that runs any script type - R, Python, PowerShell,
bash - by detecting the file extension and invoking the right interpreter
automatically. Returns the same structured \code{orchrd_result} as \code{\link[=run_cmd]{run_cmd()}}.
}
\details{
You never need to remember whether it is \code{Rscript --vanilla},
\code{pwsh -NoProfile -NonInteractive -File}, or \code{python3}. One call handles
all of them.

\strong{Interpreter resolution order:}
\enumerate{
\item The \code{interpreter} argument if explicitly supplied.
\item File extension: \code{.R}/\code{.Rmd} to \code{Rscript}, \code{.py} to \code{python3},
\code{.ps1} to \code{pwsh}, \code{.qmd} to \code{quarto}, \code{.sh} to \code{bash}, \code{.js} to \code{node}.
\item Shebang line (\verb{#!}) at the top of the file. Interpreter flags on the
shebang (e.g. \verb{#!/usr/bin/env python3 -u}) are preserved as leading args.
}
}
\examples{
\dontrun{
run_script("scripts/build_registry.R")
run_script("inst/ps/fetch.ps1", args = c("-Source", "cbn"))
run_script("etl/clean.py", args = c("--year", "2024"))
run_script("deploy.sh", env = c(ENV = "production"))
run_script("pipeline", interpreter = "python3")
run_script("report.qmd")
}
}
\seealso{
\code{\link[=run_cmd]{run_cmd()}} to run commands rather than script files.
}
