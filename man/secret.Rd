% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/secret.R
\name{secret}
\alias{secret}
\title{Retrieve a secret environment variable safely}
\usage{
secret(name, desc = NULL, default = NULL)
}
\arguments{
\item{name}{Character(1). The environment variable name.}

\item{desc}{Character(1) or NULL. Human-readable description of what this
secret is used for. Shown in the error message if the variable is
missing.}

\item{default}{Character(1) or NULL. A default value used if the variable
is not set. Setting a default suppresses the missing-variable error.
Use with caution - in production, secrets should always be explicit.}
}
\value{
An S3 object of class \code{orchrd_secret}.
}
\description{
Reads a secret from environment variables with three guarantees that
plain \code{Sys.getenv()} does not provide:
}
\details{
\enumerate{
\item Presence validation - errors immediately if the variable is not set,
with a clear message explaining what it is needed for.
\item Log safety - the value is held in a locked environment and never
surfaces through \code{as.character()}, \code{format()}, \code{print()}, \code{str()},
\code{unclass()}, or \code{dput()}. It is therefore never captured by
\code{\link[=log_step]{log_step()}} or \code{\link[=with_log]{with_log()}}, even at debug level.
\item Masked printing - the \code{orchrd_secret} object prints as
\verb{<secret: VAR_NAME>}.
}

To obtain the underlying value you must call \code{\link[=reveal]{reveal()}} explicitly. This
is intentional: it makes every point where a secret is exposed grep-able
in code review.
}
\examples{
\dontrun{
key <- secret("AZURE_KEY")
key
# <secret: AZURE_KEY>

raw <- reveal(secret("AZURE_KEY"))
key <- secret("AZURE_KEY", default = "dev-placeholder")
}
}
\seealso{
\code{\link[=reveal]{reveal()}} to extract the raw value, \code{\link[=need]{need()}} to validate
multiple secrets at once before a pipeline starts.
}
