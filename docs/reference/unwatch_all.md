# Stop all active watchers

Stops every active watcher and removes all watcher registrations.

## Usage

``` r
unwatch_all()
```

## Value

Invisibly returns the number of watchers stopped.

## See also

[`unwatch()`](https://laws2020.github.io/orchrd/reference/unwatch.md)
